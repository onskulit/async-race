export enum DecOrInc {
  decrement = 'DECREMENT',
  increment = 'INCREMENT',
}

export enum View {
  garage = 'GARAGE',
  winners = 'WINNERS',
}

export enum RaceStatus {
  init = 'INIT',
  start = 'START',
  stop = 'STOP',
}

export enum EnginePos {
  started = 'started',
  stopped = 'stopped',
  drive = 'drive',
}

export enum RaceMode {
  manual = 'MANUAL',
  race = 'RACE',
}
